Terms
-Repository
Branch
Conflict
Local
Remote

Commands
git init
git status
git add
git add .
git reset
git commit -m ''
git log or git log --oneline
git checkout {branch} 
git checkout -b{branch} (add then cd branch)
git merge {branch name}
git branch (see branches)
git branch -d {branch name} (del)
git push
git remote add {name_repository_url} {repository url} (pull data from local to remote repositorybr)
git remote {name_repository_url} {branch name}
git push -u {name_repository_url} {branch name} (push branch from local to remote repository)
git clone {repository url} (down folder)
git fetch origin 
and							(pull branch from remote to local(repository))
git checkout -b {branch name} origin/{branch name}



